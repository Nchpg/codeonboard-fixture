# demo

A deliberately tiny connection-pool library. It exists so that CodeOnboard's
ingestion — AST chunking, git blame, the GitHub API and Discussions — can be
exercised end to end against a repository small enough to hold in your head.

Every design decision here is argued in a pull request or a discussion, which is
the point: the code says *what*, the history says *why*.
